/**
 * Generates shared/config.gen.json from config.local.toml (if present,
 * gitignored) or config.example.toml (fallback). Workers and the scoring
 * engine import the generated JSON so weights/thresholds are NEVER
 * hardcoded (PROJECT_PLAN.md §21). Implemented in step 2.
 */

console.error('config:gen — implemented in step 2');
process.exit(1);
