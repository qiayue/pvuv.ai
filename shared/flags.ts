/**
 * bot_flags bit definitions (PROJECT_PLAN.md §6.2) — filled in step 2.
 * Shared by the SDK (client checks) and workers (scoring). Weights live in
 * config (config.example.toml), never here (§21).
 */

export {};
