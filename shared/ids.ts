/**
 * ID generation / cookies / HMAC (PROJECT_PLAN.md §3) — filled in step 2.
 * site_id (8-char), visitor_id/session_id (UUID), cookie names
 * _pv_id/_pv_sid/_pv_ft/_pv_v, HMAC signing for the verdict cookie.
 */

export {};
