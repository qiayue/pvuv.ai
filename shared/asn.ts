/**
 * Datacenter ASN detection (PROJECT_PLAN.md §6.1) — filled in step 3.
 * Maps cf.asn → asn_type (datacenter / residential / mobile / unknown) for
 * the 0x0008 signal and the residential/mobile trust credit.
 */

export {};
